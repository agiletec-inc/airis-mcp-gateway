name: Docker Publish

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

jobs:
  publish:
    uses: agiletec-inc/.github/.github/workflows/docker-ghcr-publish.yml@main
    with:
      image-name: airis-mcp-gateway
      dockerfile: ./apps/api/Dockerfile
      platforms: linux/amd64,linux/arm64
