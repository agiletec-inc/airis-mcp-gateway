[Unit]
Description=AIRIS MCP Gateway
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory={{REPO_ROOT}}
ExecStart=/usr/bin/docker compose -f {{REPO_ROOT}}/docker-compose.yml{{ARM_EMU_FLAG}} up -d
ExecStop=/usr/bin/docker compose -f {{REPO_ROOT}}/docker-compose.yml{{ARM_EMU_FLAG}} down
User={{USER}}
Group={{USER}}

[Install]
WantedBy=multi-user.target
